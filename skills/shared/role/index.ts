/**
 * DCA Shared — role barrel.
 *
 *   import {
 *     Role, RoleCode, AgentCode,
 *     ROLES, generic,
 *     getRole, promotedRoles, additionalRoles, allRoles, isValidRoleCode,
 *     readRoleFile, writeRoleFile, roleFilePath,
 *     resolveRole, parseRoleFlag,
 *   } from "../../shared/role";
 */
export * from "./role";
export * from "./persistence";
export * from "./resolve";
