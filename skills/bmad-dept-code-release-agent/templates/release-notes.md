# Release Notes — v{{VERSION}}

**Release date:** {{DATE}}
**Release scope:** `{{FROM_REF}}` → `{{TO_REF}}` ({{COMMIT_COUNT}} commit(s))
**Stack:** {{STACK}}
**Release manager:** {{RELEASE_MANAGER}}

---

## Summary

{{SUMMARY}}

<!-- 2-3 sentences framing the release for a stakeholder reader.
     What ships, why it matters, and the top 1-3 headline items.
     Ground in business outcomes, not implementation detail. -->

---

## Breaking changes

{{BREAKING_CHANGES_LIST}}

<!-- Empty when none. Each entry: what breaks, who's affected, migration
     path. Cross-link the ADR that authorized the break if one exists.
     Highlight prominently — this is what people scan for first. -->

---

## New features

<!-- Grouped from Conventional Commits `feat:` when
     --commit-format conventional. Otherwise the LLM buckets narrative
     commits by intent. Each entry: title + one-line description +
     PR/ticket ref + author. -->

{{FEATURES_LIST}}

---

## Fixes

<!-- Grouped from `fix:`. Same shape as Features. Call out crash /
     data-loss / security fixes at the top of the list. -->

{{FIXES_LIST}}

---

## Performance

<!-- Grouped from `perf:`. Include measured impact where the commit
     mentions it ("p99 latency -35%", "LCP -400ms"). -->

{{PERFORMANCE_LIST}}

---

## Refactoring

<!-- Grouped from `refactor:`. Kept for full traceability but usually
     rolled up as a single bullet in stakeholder-facing announcements. -->

{{REFACTORING_LIST}}

---

## Documentation

<!-- Grouped from `docs:`. Kept for completeness. -->

{{DOCUMENTATION_LIST}}

---

## CI / build changes

<!-- Grouped from `ci:` and `build:`. Only include if it affects
     downstream teams (e.g. new required build step, new deploy target). -->

{{CI_BUILD_LIST}}

---

## Contributors

{{CONTRIBUTORS_LIST}}

<!-- De-duped author list from the commit range, sorted by commit count
     descending. Format: `@username — N commit(s)`. -->

---

## Upgrade notes

{{UPGRADE_NOTES}}

<!-- Anything a downstream operator needs to do besides `git pull` +
     redeploy. Config keys added/removed/renamed, migration commands,
     order-of-operations for coordinated cutover. Empty when the
     upgrade is transparent. -->

---

## Known issues

{{KNOWN_ISSUES_LIST}}

<!-- Open issues shipping with this release that operators should know
     about. Each entry: issue link, symptom, workaround. -->

---

## Referenced tickets / PRs

{{REFERENCED_TICKETS_LIST}}

<!-- All Jira/GitHub/GitLab refs surfaced from commit trailers +
     PR-merge messages. De-duped, sorted, formatted as `[TICKET-123]` /
     `[#456]` with the target title inline. -->

---

_Generated by BMAD DCA Release agent — {{GENERATED_AT}}_
